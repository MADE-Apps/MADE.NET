<Project Sdk="MSBuild.Sdk.Extras">

  <PropertyGroup>
    <TargetFramework>uap10.0.17763</TargetFramework>
    <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
    <Product>MADE.NET Images</Product>
    <Description>
      This package includes image helpers such as:
      - StorageFileThumbnailConverter for providing the ability to show a thumbnail for a storage file.
    </Description>
    <PackageTags>MADE UI Views Controls UWP FilePicker Input File</PackageTags>
  </PropertyGroup>

  <ItemGroup Condition="'$(TargetFramework)'=='uap10.0.17763'">
    <PackageReference Include="Microsoft.Xaml.Behaviors.Uwp.Managed" Version="2.0.1" />
  </ItemGroup>

</Project>