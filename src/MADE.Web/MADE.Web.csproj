<Project Sdk="MSBuild.Sdk.Extras">

  <PropertyGroup>
    <TargetFrameworks>net5.0;net6.0</TargetFrameworks>
    <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
    <Product>MADE.NET Web</Product>
    <Description>
      This package includes:
      - HttpContextExceptionsMiddleware for handling web application exceptions with JSON responses for different exception types.
      - PaginatedRequest for providing an expected paginated request from an API controller.
      - PaginatedResponse for providing an result paginated response from an API controller request.
    </Description>
    <PackageTags>MADE Web API Requests Responses Exception Handling JSON</PackageTags>

  </PropertyGroup>

  <ItemGroup>
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Newtonsoft.Json" Version="13.0.1" />
  </ItemGroup>

</Project>